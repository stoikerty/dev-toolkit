{
  "name": "dev-toolkit-project",
  "version": "0.0.0",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test-minimal": "npm run bootstrap && bnr test-minimal"
  },
  "betterScripts": {
    "test-minimal": {
      "command": "cd examples/minimal && dev-toolkit",
      "env": {
        "NODE_ENV": "development",
        "MINIMAL_EXAMPLE": "examples/minimal"
      }
    }
  },
  "devDependencies": {
    "lerna": "^2.1.2",
    "better-npm-run": "^0.1.0"
  }
}
