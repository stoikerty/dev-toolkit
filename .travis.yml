language: node_js
sudo: false

jobs:
  include:
    - stage: Run feature-tests & lint
      script:
        - cd feature-tests
        - npm install
        - npm run test
        - npm run lint

    - stage: Lint packages & templates
      script:
        - npm run lint-all

    - stage: Test bootstrapping the project
      script:
        - npm run bootstrap
