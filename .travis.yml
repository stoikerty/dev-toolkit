language: node_js
sudo: false

jobs:
  include:
    - stage: parallel
      script:
        - echo 'Run feature-tests & lint'
        - cd feature-tests
        - npm install
        - npm run test
        - npm run lint

    - stage: parallel
      script:
        - echo 'Lint packages & templates'
        - npm install
        - npm run lint-all

    - stage: parallel
      script:
        - echo 'Test bootstrapping the project'
        - npm install
        - npm run bootstrap
