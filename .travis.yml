language: node_js
sudo: false
script:
  - echo '- Run feature tests -'
  - cd feature-tests
  - npm i && npm run test
  - echo '- Make sure bootstrapping the project works -'
  - cd ..
  - npm i && npm run bootstrap
